package {{.PackageName}}

import (
    "base/app/models"
    "base/core/module"

    "gorm.io/gorm"
)

type Module struct {
    module.DefaultModule
    DB *gorm.DB
}

func New{{.StructName}}Module(db *gorm.DB) *Module {
    return &Module{
        DB: db,
    }
}

func (m *Module) Init() error {
    return nil
}

func (m *Module) Migrate() error {
    return m.DB.AutoMigrate(&models.{{.StructName}}{})
}

func (m *Module) GetModels() []any {
    return []any{
        &models.{{.StructName}}{},
    }
}
